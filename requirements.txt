# TOP RECON — Python dependencies
# Core GUI + engine
PyQt6>=6.6
qasync>=0.27
networkx>=3.0
pyqtgraph>=0.13
httpx>=0.27
python-dotenv>=1.0

# Recon libraries (Python)
dnspython>=2.4
python-whois>=0.9
shodan>=1.31

# Optional — nicer PDF export (falls back to HTML if absent)
reportlab>=4.0

# Optional system tools (install separately, on PATH):
#   subfinder, amass, sublist3r, dnstwist, httpx, nmap, whatweb,
#   theHarvester, gitleaks, s3scanner
# Any tool that is missing simply shows "Not Installed" and is skipped.
